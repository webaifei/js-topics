/**
 * @param {string} s
 * @return {number}
 */
var lengthOfLongestSubstring = function(s) {
    let strArr = s.split('');
    for(let i = 0; i < strArr.length; i++) {
        // strArr[i]
        
    }
};